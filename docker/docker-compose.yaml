# version: '3.8'

services:
  # 前端Nginx服务
  frontend:
    image: museum_exhibition_web:${APP_VERSION}
    build:
      context: ..
      dockerfile: docker/Dockerfile
      target: frontend-nginx
    ports:
      - "8088:80"
    env_file:
      - ../ruoyi_admin/config/.env
    depends_on:
      - backend
    restart: always
    networks:
      - ragflow

  # 后端Flask服务
  backend:
    image: museum_exhibition_app:${APP_VERSION}
    build:
      context: ..
      dockerfile: docker/Dockerfile
      target: backend-runtime
    ports:
      - "9080:9000"
    env_file:
      - ../ruoyi_admin/config/.env
    environment:
      - RUOYI_ENV=prod
    restart: always
    networks:
      - ragflow

networks:
  ragflow:
    driver: bridge
